<%= form_with(model: cont_num, local: true) do |form| %>
  <% if cont_num.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cont_num.errors.count, "error") %> prohibited this cont_num from being saved:</h2>

      <ul>
      <% cont_num.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :cont_num_status %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source, id: :cont_num_source %>
  </div>

  <div class="field">
    <%= form.label :cont_num %>
    <%= form.text_field :cont_num, id: :cont_num_cont_num %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
