<%= form_with(model: cont_name, local: true) do |form| %>
  <% if cont_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cont_name.errors.count, "error") %> prohibited this cont_name from being saved:</h2>

      <ul>
      <% cont_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :cont_name_status %>
  </div>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :cont_name_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :cont_name_last_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
