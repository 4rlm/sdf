<p id="notice"><%= notice %></p>

<p>
  <strong>Status:</strong>
  <%= @url.status %>
</p>

<p>
  <strong>Url:</strong>
  <%= @url.url %>
</p>

<%= link_to 'Edit', edit_url_path(@url) %> |
<%= link_to 'Back', urls_path %>
