<%= form_with(model: addressing, local: true) do |form| %>
  <% if addressing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(addressing.errors.count, "error") %> prohibited this addressing from being saved:</h2>

      <ul>
      <% addressing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
